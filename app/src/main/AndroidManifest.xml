<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cinthyasanchez.control">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WebViewActivity" />
        <activity android:name=".WebViewActivityCerrar" />
        <activity android:name=".AyudaPP" />
        <activity android:name=".AyudaControl" />
        <activity android:name=".AyudaEstadistica" />
        <activity android:name=".AbreCierra" />
        <activity android:name=".Estadisticas" />
        <activity android:name=".RecuperarCuenta"></activity>
    </application>

</manifest>